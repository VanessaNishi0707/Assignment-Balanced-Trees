#include <iostream> // Required for input and output
using namespace std;

int main() {
    int num;
    cout << "Enter a number" << endl;
    cin >> num;

    if (num > 10) {
        cout << "That's a big number"; 
    }

    return 0;
}